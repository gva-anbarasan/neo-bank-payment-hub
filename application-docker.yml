spring:
  redis:
    host: neo-bank-redis
    port: 6379
  datasource:
    url: jdbc:postgresql://neo-bank-postgres:5432/neobank
    username: postgres
    password: postgres
  kafka:
    bootstrap-servers: neo-bank-kafka:9092

server:
  port: 8082

logging:
  level:
    com.neobank: DEBUG
